name: dockable_session_dev
description: A Flutter framework for a single persistent, minimizable session that docks to a pill above the bottom navigation bar while fully preserving its state.
version: 0.1.0
homepage: https://github.com/eq-devs/dockable_session_dev
repository: https://github.com/eq-devs/dockable_session_dev
issue_tracker: https://github.com/eq-devs/dockable_session_dev/issues

environment:
  sdk: ^3.3.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
