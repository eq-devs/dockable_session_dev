name: dockable_session_dev_example
description: Example app for the dockable_session_dev persistent session framework.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.3.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dockable_session_dev:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
